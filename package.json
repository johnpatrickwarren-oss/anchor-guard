{
  "name": "@anchor/guard",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Deterministic governance for teams running coding agents — the gate an AI agent can't talk past. Fuses Anchor's intent interview with sprag's unweakenable gate, in the agent loop.",
  "bin": {
    "guard": "bin/guard.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node test/run.mjs",
    "gate": "node bin/guard.mjs check ."
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.1",
    "@google/genai": "^2.7.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "openai": "^6.41.0"
  }
}
